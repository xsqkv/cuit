//global settings file

#pragma once

//#define add using namespace

// Global Variables
extern bool USE_PSEUDO_GRAPHICS = 1; // use pseudo graphics option
extern bool USE_SCALING = 0; // use scaling option
extern bool USE_EXTENDED_COLORS = 1; // use extended colors option NEED x256 terminal support
extern bool USE_CUSTOM_FONTS = 0; // use custom fonts option NEED terminal support
extern bool USE_SIXEL = 0; // use sixel option NEED terminal support